# Recovery / continuation report

## 原因與最小修復

本次復原首先重現 `http://127.0.0.1:8765` 連線被拒絕：原先的本機預覽服務已停止。現有 HTML、CSS、JavaScript 和素材仍完整，並非需要重建的專案。

已恢復以現有專案目錄為根目錄的本機 HTTP 預覽服務。復原過程未修改任何原有遊戲檔案；與復原前 ZIP 比對，原有 41 個檔案全部相同。新增本報告及測試紀錄，不改動遊戲系統。

本機預覽：`http://127.0.0.1:8765`。這是此電腦上的預覽服務，不是已部署的 GitHub Pages 網站；若電腦重啟或服務停止，請依 README 的本機預覽方式重新啟動。

## 已保留

- 官方船艦地圖、全部遊戲人物與原始 PNG；既有顯示遮罩與碰撞配置。
- 校服玩家、Magic Hat Girl 跟隨、鍵盤／觸控移動與靠近互動。
- 八個英語挑戰及其題數、答錯重試、組句、聽力與拖曳。
- 挑戰只解鎖寶箱、實際走到寶箱開啟後才發放一次獎勵。
- 八顆寶石與八片地圖、逐片揭露、船長最終任務、結尾。
- localStorage、自動恢復、聲音選項，以及重新開始確認。

## IMPLEMENTED AND TESTED

本次復原重新實測：

- Chrome HTTP 頁面載入、全部開場步驟、MISSION START 進入 RPG 世界。
- 實際按鍵移動，玩家位置改變且 Magic Hat Girl 跟隨；沒有阻擋執行的 JavaScript 錯誤。
- 鎖定寶箱不能領獎、解鎖不領獎、每箱只領一次、八箱對應、最終任務門檻與存檔正規化；400 組隨機題組檢查。
- Chrome 觸控模擬的方向控制、TALK、Pointer Events 拖曳及 CHECK 成功。
- 3 / 8 地圖只揭露取得的三片，剩餘區塊仍遮蔽。
- 1366 × 768、1920 × 1080、1024 × 768 的 16:9 對齊及無頁面捲動。
- 持續按住移動時，船艙家具／牆面與外船身邊界保持阻擋。
- 實際呼叫英文 speechSynthesis 及重播；未驗證實體音箱輸出。

前一輪已保存的完整通關紀錄涵蓋八個挑戰、步行到全部寶箱、重複領獎防護、完整地圖、最終題、結尾、重新整理恢復與 PLAY AGAIN。前一輪 Edge 紀錄涵蓋開場、聲音存檔、最終題答錯重試及取消重新開始保留題目。這些紀錄與本次重測分開標示，不將歷史紀錄冒稱為本次操作。

本次已再次完整通關：八個挑戰、實際步行開啟八箱、單次獎勵、地圖完成、最終題與結尾、重新整理恢復，以及 PLAY AGAIN 取消／確認全部通過，沒有 JavaScript 頁面錯誤。未發現需要修改遊戲程式的回歸問題。詳細結果記於 `tests/recovery-validation.json`。

## IMPLEMENTED BUT NOT FULLY TESTED

- Safari、實體 iPad、實體教室互動白板尚未在此次環境實測。
- 英文語音已驗證 API 呼叫；音質、音量及裝置上的語音安裝情況尚未實聽。
- 尚未部署 GitHub Pages；提供完整靜態 ZIP，部署方法在 README。
- 可選的 WebMCP 只讀進度介面未在原生支援環境驗證；不影響一般遊戲。

## PLACEHOLDER / MISSING ASSET

沒有以新繪圖或 emoji 替換官方人物與地圖。沒有獨立的單顆寶石素材，現有程式從官方寶石寶箱圖擷取顯示；沒有空開箱素材，使用官方八張開箱圖作為永久開啟狀態。沒有錄製音檔，使用瀏覽器語音。原檔透明度問題與既有處理完整列於 `ASSET_INVENTORY.md`。

## KNOWN LIMITATIONS

- 原圖有烘焙的棋盤格／黑底，執行時遮罩可能保留少量色暈；本次依 Minimal Edit 保留既有作法。
- 初次載入需要讀取與處理官方大圖，速度受電腦負載影響。
- 聽力的 Show words 是閱讀替代模式，不應視為純聽力測驗。
- 未完成的單一挑戰不保存答題中途狀態；已完成挑戰、寶箱、收集進度及位置會保存。
- 本次完整測試曾因測試工具的載入時限及虛擬時鐘同步失敗而中止，已只調整測試設定，不因此重寫遊戲。

## 交付備份

`Magic_Hat_Adventures_Recovery_Original.zip` 保留復原前檔案。

`Magic_Hat_Adventures_Working_Backup.zip` 是啟動及移動測試通過後立即建立的可運行備份；根目錄含 `index.html`、CSS、JavaScript 與全部必要素材。

最終交付 ZIP 另附本報告與測試結果，遊戲檔案保持原樣。

## 後續 UI 更新：建議通關順序

在既有介面新增 PLAY ORDER，首次開場完成後顯示，使用原角色圖片與箭頭。依物品辨認、位置詞、填空、聽力、拖曳、組句、綜合題排序，最後返回船長。支援下一步標記、已收集狀態及未開箱提醒；不變更任務 ID、寶箱配對或自由探索規則。

已驗證：首次顯示、關閉進入世界、再次開啟、移動、查看路線不改變進度，以及桌面／1024×768 平板版面；狀態與獎勵測試通過。
